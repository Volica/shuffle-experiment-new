# Spark Shuffle 算法研究报告

## 1. 研究目的
- 对比 Hash Shuffle 与 Sort Shuffle 的机制差异。
- 量化不同场景（数据规模、内存限制、数据倾斜）下的性能表现。
- 探讨各自的优缺点及适用场景。

## 2. 实验设计
- 选取计算负载与数据集。
- 设定对比指标：Shuffle数据量、内存使用、执行时间等。

## 3. 研究内容
- 算法机制深度对比（缓冲区管理 vs 内存排序/溢写）。
- 基础性能基准测试。
- 资源监控（CPU空闲率、I/O等待）。
- 内存限制边界测试。
- 数据倾斜鲁棒性测试。

## 4. 项目结构
### 4.1 设计模式
- 分派器模式（架构核心）。
- 策略模式（监控策略切换）。
- 观察者模式与工厂模式（支撑机制）。
### 4.2 项目说明
- 数据生成器（支持均匀与Zipf倾斜）。
- 实验脚本说明（基础、内存、倾斜）。

## 5. 实验
### 5.1 实验环境
- 硬件配置（3节点集群、8核CPU、动态内存）。
- 软件配置（Spark 1.6.3, Scala 2.10.5, JDK 1.8）。
### 5.2 实验负载
- 数据集：模拟用户行为日志（含1KB Payload）。
- 工作负载：`groupByKey` 强制全量 Shuffle。
### 5.3 实验步骤
- 环境搭建与配置。
- 各项测试执行流程（机制对比、基准、资源监控、内存、倾斜）。
### 5.4 实验结果与分析
#### 5.4.1 基准测试
- Hash Shuffle：文件数量爆炸导致I/O瓶颈。
- Sort Shuffle：文件合并机制带来稳定性。
#### 5.4.2 分区基数实验
- 低分区：资源竞争。
- 最佳区间：Hash Shuffle 吞吐优势。
- 高分区：Hash Shuffle 扩展性崩塌。
#### 5.4.3 CPU与负载监控
- Sort Shuffle：IO效率高，负载平稳。
- Hash Shuffle：IO等待高，负载波动剧烈。
#### 5.4.4 内存限制
- Sort Shuffle：支持磁盘溢写，低内存下仍能运行。
- Hash Shuffle：无溢写机制，低内存直接 OOM。
#### 5.4.5 数据倾斜
- 小/中数据集：Hash 略快。
- 极端倾斜（Skew 2.0）：两者均面临系统瓶颈（OOM 或 节点失联）。

## 6. 结论
- 总结不同场景下的性能差异与机制原因。
- 提出选型建议（Hash适合小规模不排序，Sort适合大规模生产环境）。

## 7. 分工
- 团队成员工作分配说明。

## 8. 不足与展望
- 针对极端倾斜场景的优化空间。

## 9. 遇到的问题
- 实验过程中遇到的 OOM 及超时问题与解决尝试。